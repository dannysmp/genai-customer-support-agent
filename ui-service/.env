# =============================================================================
# UI Service - Environment Configuration File
# =============================================================================
# This file defines environment variables used by the UI Service and Docker
# runtime. It allows flexible configuration for connecting the frontend to the
# backend customer support service across different environments.
#
# SECURITY NOTE:
# - Never commit this file with production URLs or sensitive data to version control.
# - For production deployments, use secure secret management systems such as:
#   AWS Secrets Manager, GCP Secret Manager, or HashiCorp Vault.
# =============================================================================

# -----------------------------------------------------------------------------
# Backend Connection
# -----------------------------------------------------------------------------

# Base URL of the backend API used for communication with the customer-support-service.
# For local development, this typically points to http://localhost:8000
BACKEND_BASE_URL=http://localhost:8000

# -----------------------------------------------------------------------------
# Streamlit Configuration (Optional)
# -----------------------------------------------------------------------------

# Server port for the Streamlit application (default: 8501)
# Uncomment and modify if you need to use a custom port.
# STREAMLIT_SERVER_PORT=8501

# Enable Streamlit to listen on all network interfaces inside the container.
# This setting is required for Docker deployments.
STREAMLIT_SERVER_ADDRESS=0.0.0.0